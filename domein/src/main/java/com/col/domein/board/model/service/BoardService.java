package com.col.domein.board.model.service;

import java.util.List;
import java.util.Map;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.col.domein.board.model.dao.BoardDao;

@Service
public interface BoardService {

	/*
	 * List<Map> selectProductList(int cPage, int numPerpage); List<Map<String,
	 * Object>> list(Map<String, Object> map);
	 * 
	 * List<Map<String,Object>> selectBoardList(Map<String,Object> map) throws
	 * Exception;
	 * 
	 * List<Map> selectBoardList(int cPage, int numPerpage);
	 */
}
